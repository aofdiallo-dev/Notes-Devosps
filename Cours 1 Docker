🐳 Docker — Cours 1
Comprendre Docker + installation sur Ubuntu + premier container

On va partir de zéro, comme si tu n'avais jamais utilisé Docker. L'objectif de ce premier cours est que tu comprennes réellement ce que tu fais, et pas seulement que tu copies des commandes.

1. Qu'est-ce que Docker ?

Docker est une plateforme qui permet de packager et exécuter des applications dans des containers.

Imagine que tu développes une application qui nécessite :

Application
├── Python 3.12
├── PostgreSQL
├── Nginx
├── certaines librairies
└── certaines configurations

Sur ton ordinateur, tout fonctionne.

Mais tu donnes ton projet à quelqu'un d'autre et il obtient :

« Ça ne fonctionne pas chez moi. »

C'est un problème classique en développement.

Docker permet de créer un environnement reproductible contenant l'application et ce dont elle a besoin.

2. Docker ≠ machine virtuelle

C'est une notion très importante pour les entretiens DevOps.

Machine virtuelle
              Serveur physique
                    │
              ┌─────▼─────┐
              │ Hypervisor│
              └─────┬─────┘
                    │
       ┌────────────┼────────────┐
       ▼            ▼            ▼
    VM Linux      VM Linux     VM Windows
       │            │            │
   OS complet    OS complet   OS complet
       │            │            │
     App            App          App

Chaque VM possède généralement son propre système d'exploitation invité.

Exemples :

VMware ESXi
VirtualBox
Hyper-V
Proxmox
Container Docker
             Serveur Ubuntu
                  │
             Docker Engine
                  │
       ┌──────────┼──────────┐
       ▼          ▼          ▼
   Container   Container   Container
      │           │           │
     App         Nginx      PostgreSQL

Les containers partagent le kernel du système hôte, contrairement aux VM classiques.

Cela les rend généralement beaucoup plus légers et rapides à démarrer.

3. Les 4 notions fondamentales

Avant de commencer les commandes, retiens ceci :

Dockerfile
    │
    ▼
  IMAGE
    │
    │ docker run
    ▼
CONTAINER
    │
    ▼
Application

Et il existe aussi :

Docker Hub
    │
    ▼
Images disponibles
    │
    ▼
docker pull
    │
    ▼
Image locale
3.1 Image

Une image Docker est un modèle servant à créer un container.

Par exemple :

ubuntu
nginx
postgres
redis
python
node

Une image n'est pas directement ton application en cours d'exécution.

Elle sert de base.

3.2 Container

Un container est une instance en fonctionnement d'une image.

Par exemple :

docker run ubuntu

Docker prend l'image ubuntu et crée un container à partir d'elle.

On peut avoir :

Image Ubuntu
     │
     ├── Container 1
     ├── Container 2
     └── Container 3

Une seule image peut donc servir à créer plusieurs containers.

4. Installer Docker sur Ubuntu

Nous allons utiliser la méthode officielle basée sur le dépôt Docker.

Commence par ouvrir ton terminal Ubuntu.

Étape 1 — Vérifier Ubuntu
cat /etc/os-release

Tu devrais obtenir quelque chose ressemblant à :

NAME="Ubuntu"
VERSION="24.04 LTS"

Tu peux également utiliser :

uname -a
5. Mettre Ubuntu à jour
sudo apt update
Que fait cette commande ?

Elle met à jour la liste des paquets disponibles.

⚠️ Elle n'installe pas les mises à jour.

C'est simplement :

Serveurs Ubuntu
      │
      ▼
Nouvelle liste des paquets
      │
      ▼
Ubuntu local

Ensuite :

sudo apt upgrade

Cette commande installe les mises à jour disponibles.

6. Installer les prérequis

Nous allons installer les outils nécessaires pour utiliser le dépôt Docker :

sudo apt install ca-certificates curl

Explication :

Commande/paquet	Utilité
apt	gestionnaire de paquets Ubuntu
ca-certificates	certificats HTTPS
curl	télécharger/récupérer des données via HTTP/HTTPS
sudo	exécuter avec les privilèges administrateur
7. Ajouter la clé du dépôt Docker

Créer le dossier :

sudo install -m 0755 -d /etc/apt/keyrings

Puis récupérer la clé officielle Docker :

sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
-o /etc/apt/keyrings/docker.asc

Puis :

sudo chmod a+r /etc/apt/keyrings/docker.asc

Pourquoi ?

Parce qu'APT doit pouvoir vérifier cryptographiquement les paquets provenant du dépôt Docker.

8. Ajouter le dépôt Docker

Exécute :

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

Puis :

sudo apt update

À partir de maintenant, Ubuntu connaît le dépôt officiel Docker.

9. Installer Docker

Installe Docker Engine et les composants courants :

sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

Tu installes notamment :

docker-ce
    │
    └── Docker Engine

docker-ce-cli
    │
    └── commande docker

containerd.io
    │
    └── runtime de containers

docker-buildx-plugin
    │
    └── construction avancée d'images

docker-compose-plugin
    │
    └── Docker Compose
10. Vérifier l'installation

Première commande :

docker --version

Puis :

docker compose version

Tu dois obtenir une version Docker et une version Docker Compose.

11. Vérifier le service Docker
sudo systemctl status docker

Tu devrais voir quelque chose comme :

Active: active (running)

Cela signifie que le Docker Engine fonctionne.

12. Comprendre systemctl

Cette commande sera très utile pour ton apprentissage Linux/DevOps.

systemctl status docker

signifie :

« Quel est l'état du service Docker ? »

Tu peux aussi faire :

sudo systemctl start docker

pour démarrer Docker.

sudo systemctl stop docker

pour arrêter Docker.

sudo systemctl restart docker

pour redémarrer Docker.

Et :

sudo systemctl enable docker

pour faire démarrer Docker automatiquement avec Ubuntu.

13. Premier problème possible : permission denied

Si tu exécutes :

docker ps

et que tu obtiens une erreur du type :

permission denied

c'est généralement parce que ton utilisateur n'a pas encore accès au socket Docker.

Tu peux ajouter ton utilisateur au groupe docker :

sudo usermod -aG docker $USER

Puis déconnecte-toi et reconnecte-toi à ta session Ubuntu.

Ensuite :

docker ps

Tu ne devrais plus avoir besoin de sudo.

⚠️ Point important pour la sécurité : le groupe docker donne des privilèges très élevés sur la machine. Nous reviendrons sur ce sujet lorsque nous étudierons la sécurité Docker.

14. Notre premier container 🚀

Maintenant, nous allons vraiment utiliser Docker.

Exécute :

docker run hello-world

C'est probablement la commande Docker la plus importante de ce premier cours.

Que se passe-t-il ?

docker run hello-world
       │
       ▼
Docker cherche l'image
       │
       ▼
Image absente localement ?
       │
       ▼
docker pull
       │
       ▼
Téléchargement de hello-world
       │
       ▼
Création du container
       │
       ▼
Exécution
       │
       ▼
Message "Hello from Docker!"
15. Vérifier l'image téléchargée

Exécute :

docker images

Tu devrais voir une ligne contenant :

hello-world

La commande :

docker images

affiche les images présentes sur ta machine.

16. Voir les containers

Maintenant :

docker ps

Tu risques de ne rien voir.

Pourquoi ?

Parce que docker ps affiche principalement les containers actuellement en fonctionnement.

Notre container hello-world a terminé son programme.

Pour voir également les containers arrêtés :

docker ps -a

Tu devrais voir quelque chose comme :

CONTAINER ID
IMAGE
COMMAND
STATUS
NAMES
17. Très important : docker ps vs docker ps -a

Retient cette différence :

docker ps

➡️ containers actuellement actifs.

docker ps -a

➡️ containers actifs + arrêtés.

C'est une question classique en entretien Docker.

18. Lancer un vrai container

Maintenant nous allons lancer Ubuntu.

docker run -it ubuntu bash

Regardons la commande :

docker
  │
  └── commande Docker

run
  │
  └── créer et démarrer un container

-it
  │
  ├── -i = interactive
  └── -t = terminal

ubuntu
  │
  └── image utilisée

bash
  │
  └── programme lancé dans le container

Tu devrais maintenant obtenir quelque chose comme :

root@xxxxxxxx:/#

🎉 Tu es maintenant à l'intérieur d'un container Ubuntu.

19. Vérifier que tu es dans le container

Exécute :

cat /etc/os-release

Tu verras les informations Ubuntu du container.

Puis :

hostname

Tu verras le nom/ID du container.

Tu peux également faire :

ls
20. Container ≠ ordinateur virtuel

C'est ici qu'il faut bien comprendre la différence.

Lorsque tu fais :

docker run -it ubuntu bash

tu as l'impression d'être dans un petit ordinateur.

Mais ce n'est pas une VM complète.

Le container utilise notamment le kernel de la machine hôte.

C'est l'une des raisons pour lesquelles Docker est très léger comparé à une VM traditionnelle.

21. Sortir du container

Tape :

exit

Tu reviens à ton Ubuntu principal.

Puis :

docker ps

Le container n'est plus actif.

Mais :

docker ps -a

va toujours l'afficher.

22. Démarrer un container existant

Tu peux voir son nom avec :

docker ps -a

Supposons que Docker lui ait donné le nom :

friendly_name

Tu peux le démarrer :

docker start friendly_name

Puis :

docker ps
23. Arrêter un container
docker stop friendly_name
24. Supprimer un container
docker rm friendly_name

Attention :

docker rm

supprime le container, pas nécessairement l'image.

Donc :

IMAGE
 │
 ├── Container A
 ├── Container B
 └── Container C

Si tu supprimes :

docker rm ContainerA

l'image reste disponible.

25. Supprimer une image

Pour supprimer une image :

docker rmi ubuntu

ou :

docker image rm ubuntu
26. Les commandes à mémoriser aujourd'hui

Voici ta première mini Cheat Sheet Docker :

Commande	Fonction
docker --version	version Docker
docker ps	containers actifs
docker ps -a	tous les containers
docker images	images locales
docker pull IMAGE	télécharger une image
docker run IMAGE	créer + démarrer un container
docker start CONTAINER	démarrer
docker stop CONTAINER	arrêter
docker restart CONTAINER	redémarrer
docker rm CONTAINER	supprimer container
docker rmi IMAGE	supprimer image
docker exec	exécuter une commande dans un container
docker logs	afficher les logs
27. Ton premier exercice 🧪

Ne passe pas encore au Dockerfile.

Fais exactement cet exercice dans ton Ubuntu.

Étape 1
docker pull nginx
Étape 2
docker images
Étape 3

Lance Nginx :

docker run -d --name mon-nginx nginx
Étape 4

Vérifie :

docker ps
Étape 5

Regarde les logs :

docker logs mon-nginx
Étape 6

Arrête Nginx :

docker stop mon-nginx
Étape 7

Vérifie :

docker ps
Étape 8

Puis :

docker ps -a

Tu devrais maintenant voir la différence entre container actif et container arrêté.

🧠 Ce que tu dois absolument comprendre après le cours 1

Tu dois être capable d'expliquer cette chaîne :

        Docker Hub
            │
            │ docker pull
            ▼
         IMAGE
            │
            │ docker run
            ▼
       CONTAINER
            │
            ▼
       APPLICATION

Et surtout :

Une image est un modèle. Un container est une instance créée à partir de cette image.

Exemple :

          nginx IMAGE
              │
       ┌──────┼──────┐
       ▼      ▼      ▼
    nginx-1 nginx-2 nginx-3
   container container container





🐳 Docker — Cours 2
docker run en profondeur : ports, noms, mode détaché, variables et commandes dans un container

Dans le cours 1, tu as appris la différence entre image et container.

Aujourd'hui, on va comprendre une commande essentielle de Docker :

docker run

À la fin du cours, tu sauras lancer un serveur Nginx, accéder à sa page depuis ton navigateur et comprendre exactement ce que fait chaque option.

1. Rappel : que fait docker run ?

Quand tu écris :

docker run nginx

Docker effectue essentiellement ceci :

Image nginx
    │
    ▼
Création du container
    │
    ▼
Démarrage du container
    │
    ▼
Processus Nginx

Donc :

docker run IMAGE

signifie :

Créer un nouveau container à partir d'une image et le démarrer.

2. docker run -d

Essayons :

docker run -d nginx

Le -d signifie :

-d = detached

Autrement dit :

Exécuter le container en arrière-plan.

Sans -d :

docker run nginx

ton terminal reste attaché au processus.

Avec :

docker run -d nginx

Docker te rend immédiatement le terminal.

Tu peux alors faire :

docker ps

Tu devrais voir ton Nginx.

3. Donner un nom au container

Par défaut, Docker peut générer un nom aléatoire :

happy_turing
interesting_morse
focused_babbage

Ce n'est pas très pratique.

On peut choisir le nom :

docker run -d --name mon-nginx nginx

La structure est :

docker run
    │
    ├── -d
    │    └── arrière-plan
    │
    ├── --name mon-nginx
    │    └── nom du container
    │
    └── nginx
         └── image

Maintenant :

docker ps

devrait afficher :

mon-nginx
4. Pourquoi --name est important ?

Compare :

docker stop interesting_morse

avec :

docker stop mon-nginx

Le deuxième est beaucoup plus facile à retenir.

Dans un vrai projet DevOps, tu peux avoir :

nginx
backend
frontend
postgres
redis

Donc nommer correctement tes containers devient très utile.

5. Le problème des ports 🔌

Maintenant nous avons Nginx qui tourne :

docker ps

Mais essayons d'accéder à :

http://localhost

Il est possible que tu n'obtiennes pas la page Nginx.

Pourquoi ?

Parce que le port du container n'est pas automatiquement publié sur le port de ta machine.

C'est une notion extrêmement importante.

6. Port du container vs port de l'hôte

Imagine :

Ubuntu
┌─────────────────────────────────┐
│                                 │
│      Port 8080                  │
│          │                      │
│          ▼                      │
│    ┌──────────────┐             │
│    │    Docker    │             │
│    │              │             │
│    │ ┌──────────┐ │             │
│    │ │  Nginx   │ │             │
│    │ │ Port 80   │ │             │
│    │ └──────────┘ │             │
│    └──────────────┘             │
│                                 │
└─────────────────────────────────┘

Nginx écoute généralement sur :

port 80

à l'intérieur du container.

Mais ton Ubuntu doit savoir :

« Lorsque quelqu'un arrive sur le port 8080 de ma machine, envoie-le vers le port 80 du container. »

C'est exactement le rôle de :

-p
7. -p : publier un port

Arrête et supprime ton ancien container :

docker stop mon-nginx

Puis :

docker rm mon-nginx

Lance maintenant :

docker run -d --name mon-nginx -p 8080:80 nginx

Regarde attentivement :

-p 8080:80
   │     │
   │     └── port du container
   │
   └──────── port de l'hôte

Donc :

Ubuntu:8080
     │
     ▼
Docker
     │
     ▼
Container:80
     │
     ▼
Nginx
8. Tester dans le navigateur 🌐

Sur la machine Ubuntu où Docker fonctionne, ouvre :

http://localhost:8080

Tu devrais voir la page :

Welcome to nginx!

🎉 Tu viens de faire ton premier port mapping Docker.

9. Vérifier le mapping

Exécute :

docker ps

Tu devrais avoir quelque chose ressemblant à :

PORTS
0.0.0.0:8080->80/tcp

Cela signifie :

8080 sur Ubuntu
      ↓
80 dans le container
10. Tester avec curl

Au lieu du navigateur :

curl http://localhost:8080

Tu devrais recevoir le HTML de la page Nginx.

Tu peux également faire :

curl -I http://localhost:8080

Tu verras notamment une réponse HTTP du type :

HTTP/1.1 200 OK
Server: nginx
11. docker port

Docker possède également une commande pratique :

docker port mon-nginx

Tu devrais obtenir quelque chose comme :

80/tcp -> 0.0.0.0:8080
12. Modifier le port

Tu pourrais décider d'utiliser le port 8081 :

docker run -d --name nginx2 -p 8081:80 nginx

Tu auras donc :

http://localhost:8081

Et tu pourrais avoir :

nginx1 → 8080 → port 80
nginx2 → 8081 → port 80
nginx3 → 8082 → port 80

Chaque container possède son propre espace réseau.

13. Une erreur classique

Si tu fais :

docker run -d --name nginx2 -p 8080:80 nginx

alors que mon-nginx utilise déjà :

8080

Docker va refuser le lancement parce que le port 8080 de l'hôte est déjà utilisé.

Tu pourrais alors utiliser :

docker run -d --name nginx2 -p 8081:80 nginx
14. docker logs

Les logs sont extrêmement importants en DevOps.

docker logs mon-nginx

Pour suivre les logs en temps réel :

docker logs -f mon-nginx

Le -f signifie :

follow

C'est très utile lorsqu'une application démarre et rencontre une erreur.

15. docker exec

Maintenant nous allons entrer dans notre container.

docker exec -it mon-nginx bash

Mais attention : certaines images très légères ne possèdent pas bash.

Dans ce cas :

docker exec -it mon-nginx sh

Pour Nginx, bash peut être disponible selon l'image utilisée, mais sh est le choix le plus universel.

16. Comprendre exec -it
docker exec -it mon-nginx sh

Décomposons :

docker
  │
  └── Docker

exec
  │
  └── exécuter une commande

-i
  │
  └── interaction avec le terminal

-t
  │
  └── pseudo-terminal

mon-nginx
  │
  └── container cible

sh
  │
  └── shell à exécuter

Tu arrives alors dans :

/ #

Tu es à l'intérieur du container.

17. Explorer le container

Une fois dedans :

ls

Puis :

cd /usr/share/nginx/html

Et :

ls

Tu devrais trouver notamment :

index.html

Tu peux afficher le contenu :

cat index.html

C'est la page Nginx que ton navigateur reçoit.

18. Sortir du container

Tape :

exit

Tu reviens sur Ubuntu.

19. docker inspect

Une commande très importante pour le dépannage :

docker inspect mon-nginx

Docker va afficher énormément d'informations :

ID du container
image
réseau
IP
ports
volumes
configuration
variables d'environnement
état
etc.

Tu peux voir certaines informations réseau plus facilement avec :

docker inspect mon-nginx

et rechercher la section :

NetworkSettings
20. docker stats

Docker permet également de voir la consommation des containers :

docker stats

Tu verras notamment :

CONTAINER
CPU %
MEM USAGE
MEM %
NET I/O
BLOCK I/O

C'est très intéressant pour le monitoring.

21. Les variables d'environnement -e

Une autre option importante :

-e

Elle permet de définir une variable d'environnement.

Exemple :

docker run -d \
  --name test \
  -e ENV=production \
  nginx

À l'intérieur du container :

echo $ENV

donnera :

production

Les variables d'environnement deviennent particulièrement importantes avec PostgreSQL, Node.js, Python et Docker Compose.

22. Exemple PostgreSQL

Plus tard nous utiliserons quelque chose comme :

docker run -d \
  --name postgres \
  -e POSTGRES_PASSWORD=monmotdepasse \
  postgres

Tu vois déjà apparaître une architecture DevOps réelle :

Nginx
  │
  ▼
Application
  │
  ▼
PostgreSQL

Nous approfondirons PostgreSQL lorsque nous verrons les volumes, car une base de données doit conserver ses données même si le container est supprimé.

23. --restart

Docker peut également redémarrer automatiquement un container.

Exemple :

docker run -d \
  --name mon-nginx \
  --restart unless-stopped \
  -p 8080:80 \
  nginx

Quelques politiques courantes :

no
always
on-failure
unless-stopped

Par exemple :

--restart unless-stopped

signifie en gros :

Redémarrer automatiquement le container après un arrêt/restart de Docker, sauf si tu l'as explicitement arrêté.

24. Ta commande Docker complète

Voici une commande que tu dois maintenant savoir lire :

docker run -d \
  --name mon-nginx \
  --restart unless-stopped \
  -p 8080:80 \
  nginx

Elle signifie :

docker run
│
├── -d
│   └── arrière-plan
│
├── --name mon-nginx
│   └── nom du container
│
├── --restart unless-stopped
│   └── politique de redémarrage
│
├── -p 8080:80
│   ├── Ubuntu : 8080
│   └── Container : 80
│
└── nginx
    └── image utilisée
25. 🧪 TP du cours 2

Maintenant, ne te contente pas de lire. Fais le laboratoire.

Étape 1 — Nettoyage
docker rm -f mon-nginx

Le -f force la suppression du container même s'il fonctionne.

Étape 2 — Télécharger Nginx
docker pull nginx
Étape 3 — Créer le container
docker run -d \
  --name mon-nginx \
  --restart unless-stopped \
  -p 8080:80 \
  nginx
Étape 4 — Vérifier
docker ps

Tu dois trouver :

mon-nginx

et :

0.0.0.0:8080->80/tcp
Étape 5 — Tester
curl http://localhost:8080
Étape 6 — Regarder les logs
docker logs mon-nginx

Puis :

docker logs -f mon-nginx

Fais Ctrl+C pour arrêter l'affichage des logs.

Étape 7 — Entrer dans le container
docker exec -it mon-nginx sh

Puis :

hostname
ls /usr/share/nginx/html
cat /usr/share/nginx/html/index.html

Puis :

exit
Étape 8 — Examiner le container
docker inspect mon-nginx
Étape 9 — Voir les ressources
docker stats
🧠 Cheat Sheet — Cours 2
# Lancer en arrière-plan
docker run -d IMAGE

# Donner un nom
docker run -d --name NAME IMAGE

# Publier un port
docker run -d -p 8080:80 IMAGE

# Définir une variable
docker run -e VARIABLE=VALEUR IMAGE

# Redémarrage automatique
docker run --restart unless-stopped IMAGE

# Voir les containers
docker ps

# Voir tous les containers
docker ps -a

# Voir les logs
docker logs CONTAINER

# Suivre les logs
docker logs -f CONTAINER

# Entrer dans un container
docker exec -it CONTAINER sh

# Informations détaillées
docker inspect CONTAINER

# Consommation ressources
docker stats

# Arrêter
docker stop CONTAINER

# Supprimer
docker rm CONTAINER
🎯 Ce que tu dois retenir

Les 5 notions essentielles du cours 2 sont :

1. -d
docker run -d nginx

➡️ Exécuter en arrière-plan.

2. --name
--name mon-nginx

➡️ Donner un nom facilement identifiable.

3. -p
-p 8080:80

➡️ Faire correspondre :

PORT HÔTE : PORT CONTAINER
4. docker exec
docker exec -it mon-nginx sh

➡️ Exécuter une commande dans un container actif.

5. docker logs
docker logs mon-nginx

➡️ Voir ce que produit l'application.

🔜 Cours 3 : Dockerfile

Maintenant que tu sais utiliser une image existante, nous allons apprendre à construire notre propre image.

Nous partirons de :

Dockerfile
     │
     ▼
docker build
     │
     ▼
IMAGE PERSONNALISÉE
     │
     ▼
docker run
     │
     ▼
CONTAINER
















